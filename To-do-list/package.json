{
  "name": "to-do-list",
  "version": "1.0.0",
  "description": "A to do list app that stores each todo in its own file on the server",
  "scripts": {
    "start": "npx nodemon ./server.js",
    "clean": "rm -rf datastore/data/* datastore/counter.txt",
    "lint": "eslint ./",
    "test": "mocha test"
  },
  "dependencies": {
    "bluebird": "^3.7.2",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "sprintf-js": "^1.1.2",
    "underscore": "^1.11.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "nodemon": "^2.0.6"
  }
}
